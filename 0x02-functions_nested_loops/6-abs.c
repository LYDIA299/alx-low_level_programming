#include "main.h"

/**
 *_abs - computes the absolute number meaning it returns positives
 *@n: parameter
 *
 *Return: the absolute value of (n)
 */

int _abs(int n)
{
if (n < 0)
return (n * -1);
else
return (n);
}
