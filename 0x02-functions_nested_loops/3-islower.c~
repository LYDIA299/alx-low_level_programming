#include "main.h"

/**
 *_islower - checks if the argument is lowercase or not
 *@c: is the parament
 *
 *Return: 1 (success) and 0 (fail)
 */

int _islower(int c)
{
unsigned char i;
char d;
i = c;
d = 'a';
while (d <= 'z')
{
if (i == d)
return (1);
d++;
}
return (0);

}
