#include "main.h"

/**
 *add - adds two integers
 *Description: it takes in two arguments x and y
 *@x: first parameter
 *@y: second parameter
 *Return: addition of two numbers
 */

int add(int x, int y)
{
int m = x + y;
return (m);
}
