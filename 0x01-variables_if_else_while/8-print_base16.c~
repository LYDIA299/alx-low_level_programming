#include <stdio.h>

/**
 *main - Entry point
 *
 *Return: 0 always
 */

int main(void)
{
int n;
char c;

for (n = 47; n < 58; n++)
putchar(n);
for (c = 'a'; c <= 'f'; c++)
putchar(c);
putchar('\n');

return (0);
}
