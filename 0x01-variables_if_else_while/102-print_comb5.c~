#include <stdio.h>

/**
 *main - Entry point
 *
 *Return: Always 0 (success)
 */

int main(void)
{
int i, j, k, m;

for (i = 48; i < 58; i++)
{
for (j = 48; j < 57; j++)
{
for (k = i; k < 58; k++)
{
  /**
   *if (k == 48)
   *m = 49;
   *else
   *m = 48;
   */
m = 48;
while (m < 58)
{
putchar(i);
putchar(j);
putchar(' ');
putchar(k);
putchar(m);
m++;
if ((j != 56)  && (m != 57))
{
putchar(',');
putchar(' ');
}
}
}
}
}
putchar('\n');

return (0);
}
